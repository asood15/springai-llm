spring:
  application:
    name: chatmodels


  ai:
    chat:
      client:
        enabled: false

    openai:
      api-key=${OPENAI_API_KEY}

    anthropic:
      api-key: ${ANTHROPIC_API_KEY}

    ollama.chat.model: gemma3:270m